start
    arr dummy 20 1 =;
    str source = "source string";
    int index = 0;
    while source[index] > 0; lstart
        dummy[index] = source[index];
        index = index + 1;
    lend

    syscall 4 1 dummy 13;
exit 1;